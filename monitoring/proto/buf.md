# API

this module contains all the APIs required to interacts with the monitoring service.

## Structure

```log
proto/
├── buf.md
├── buf.yaml
├── google
│   └── type
│       └── datetime.proto
└── monitoring
    └── v1
        └── logs.proto
        └── metrics.proto
        └── traces.proto
```